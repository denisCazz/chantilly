---
import { Icon } from 'astro-icon/components';
---

<section id="chi-siamo" class="about section">
	<div class="container">
		<div class="about-content">
			<div class="about-text">
				<h2 class="section-title">Chi Siamo</h2>
				<p class="lead">
					Dal 1988 il Bar Chantilly è un punto di riferimento per la comunità di Carmagnola, offrendo un servizio di qualità in un ambiente accogliente e familiare.
				</p>
				<div class="features">
					<div class="feature">
						<Icon name="mdi:coffee" />
						<div>
							<h3>Caffetteria di Qualità</h3>
							<p>Serviamo solo i migliori caffè italiani, preparati con cura e passione per iniziare al meglio la tua giornata.</p>
						</div>
					</div>
					<div class="feature">
						<Icon name="mdi:smoking" />
						<div>
							<h3>Tabaccheria Autorizzata</h3>
							<p>Punto vendita autorizzato per tabacchi, sigarette e tutti gli accessori per fumatori, con un vasto assortimento sempre disponibile.</p>
						</div>
					</div>
					<div class="feature">
						<Icon name="mdi:account-group" />
						<div>
							<h3>Un Luogo di Incontro</h3>
							<p>Più di un semplice bar: siamo il punto di ritrovo della comunità, dove tradizione e modernità si incontrano ogni giorno.</p>
						</div>
					</div>
				</div>
			</div>
			<div class="about-stats">
				<div class="stat">
					<Icon name="mdi:calendar" />
					<div class="stat-content">
						<span class="stat-number">27</span>
						<span class="stat-label">Anni di Storia</span>
					</div>
				</div>
				<div class="stat">
					<Icon name="mdi:coffee-outline" />
					<div class="stat-content">
						<span class="stat-number">250+</span>
						<span class="stat-label">Caffè al Giorno</span>
					</div>
				</div>
				<div class="stat">
					<Icon name="mdi:heart" />
					<div class="stat-content">
						<span class="stat-number">100%</span>
						<span class="stat-label">Soddisfazione</span>
					</div>
				</div>
			</div>
		</div>
	</div>
</section>

<style>
	.about {
		background: var(--color-background);
		padding: 6rem 0;
		border-bottom: 1px solid var(--color-border);
	}

	[data-theme="dark"] .about {
		background: var(--color-surface);
	}

	.about-content {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 4rem;
		align-items: start;
	}

	.about-text {
		font-size: 1.1rem;
		line-height: 1.8;
		color: var(--color-text-secondary);
	}

	.about-text p {
		margin-bottom: 1.5rem;
	}

	.features {
		display: grid;
		grid-template-columns: 1fr;
		gap: 1.5rem;
	}

	.feature {
		display: flex;
		align-items: flex-start;
		gap: 1.5rem;
		padding: 1.5rem;
		background: var(--color-bg);
		border-radius: var(--border-radius-lg);
		border: 1px solid var(--color-border);
		box-shadow: var(--shadow-sm);
		transition: var(--transition);
	}

	[data-theme="dark"] .feature {
		background: var(--color-bg-dark);
		border-color: var(--color-border-dark);
	}

	.feature:hover {
		transform: translateY(-5px);
		box-shadow: var(--shadow-md);
		border-color: var(--color-secondary);
	}

	.feature [data-icon] {
		color: var(--color-accent);
		font-size: 2.5rem;
		flex-shrink: 0;
	}

	.feature h3 {
		font-size: 1.25rem;
		color: var(--color-text);
		margin-bottom: 0.5rem;
		font-weight: 600;
	}

	.feature p {
		color: var(--color-text-offset);
		font-size: 1rem;
		font-weight: 400;
		margin: 0;
	}

	.about-stats {
		display: grid;
		grid-template-columns: 1fr;
		gap: 2rem;
		padding: 2rem;
		background: var(--color-bg-offset);
		border-radius: var(--border-radius-lg);
		border: 1px solid var(--color-border);
	}

	[data-theme="dark"] .about-stats {
		background: var(--color-bg-dark-offset);
		border-color: var(--color-border-dark);
	}

	.stat {
		display: flex;
		align-items: center;
		gap: 1rem;
		padding: 1rem;
		background: var(--color-bg);
		border-radius: var(--border-radius);
		border: 1px solid var(--color-border);
	}

	[data-theme="dark"] .stat {
		background: var(--color-bg-dark);
		border-color: var(--color-border-dark);
	}

	.stat-content {
		display: flex;
		flex-direction: column;
		gap: 0.25rem;
	}

	.stat [data-icon] {
		color: var(--color-accent);
		font-size: 2rem;
		flex-shrink: 0;
	}

	.stat-number {
		font-size: 1.75rem;
		color: var(--color-secondary);
		font-weight: 600;
		line-height: 1;
	}

	.stat-label {
		font-size: 0.875rem;
		color: var(--color-text-offset);
		font-weight: 400;
	}

	@media (max-width: 992px) {
		.about-content {
			grid-template-columns: 1fr;
			gap: 3rem;
		}
		
		.about-stats {
			grid-template-columns: 1fr;
		}
	}

	@media (max-width: 768px) {
		.about {
			padding: 4rem 0;
		}
		
		.feature {
			flex-direction: column;
			text-align: center;
			gap: 1rem;
			align-items: center;
		}
		
		.about-stats {
			padding: 1.5rem;
		}
	}
</style>
