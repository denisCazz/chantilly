---
---

<section id="orari" class="hours section">
	<div class="container">
		<h2 class="section-title">Orari di Apertura</h2>
		<p class="section-subtitle">
			Siamo aperti tutti i giorni per servirti al meglio, tranne il mercoledì
		</p>
		
		<div class="hours-content">
			<div class="hours-table">
				<div class="hours-row">
					<span class="day">Lunedì</span>
					<span class="time">07:00 - 19:30</span>
				</div>
				<div class="hours-row">
					<span class="day">Martedì</span>
					<span class="time">07:00 - 19:30</span>
				</div>
				<div class="hours-row closed">
					<span class="day">Mercoledì</span>
					<span class="time">Chiuso</span>
				</div>
				<div class="hours-row">
					<span class="day">Giovedì</span>
					<span class="time">07:00 - 19:30</span>
				</div>
				<div class="hours-row">
					<span class="day">Venerdì</span>
					<span class="time">07:00 - 19:30</span>
				</div>
				<div class="hours-row">
					<span class="day">Sabato</span>
					<span class="time">07:00 - 19:30</span>
				</div>
				<div class="hours-row">
					<span class="day">Domenica</span>
					<span class="time">08:00 - 13:00</span>
				</div>
			</div>
			
			<div class="hours-info">
				<div class="info-card">
					<div class="info-icon">🕐</div>
					<h3>Orario Continuato</h3>
					<p>Aperto tutto il giorno senza pausa pranzo dal lunedì al sabato</p>
				</div>
				
				<div class="info-card">
					<div class="info-icon">☀️</div>
					<h3>Apertura Mattutina</h3>
					<p>Iniziamo presto per la tua colazione perfetta</p>
				</div>
				
				<div class="info-card">
					<div class="info-icon">📅</div>
					<h3>Orari Festivi</h3>
					<p>Gli orari potrebbero variare durante le festività. Contattaci per informazioni</p>
				</div>
			</div>
		</div>
	</div>
</section>

<style>
	.hours {
		background: var(--color-white);
	}
	
	.hours-content {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 4rem;
		align-items: start;
	}
	
	.hours-table {
		background: var(--color-white);
		border: 2px solid var(--color-gray);
		border-radius: var(--border-radius-lg);
		overflow: hidden;
		box-shadow: var(--shadow);
	}
	
	.hours-row {
		display: flex;
		justify-content: space-between;
		align-items: center;
		padding: 1.25rem 1.5rem;
		border-bottom: 1px solid var(--color-gray);
		transition: var(--transition);
	}
	
	.hours-row:last-child {
		border-bottom: none;
	}
	
	.hours-row:hover {
		background: var(--color-warm);
	}
	
	.hours-row.closed {
		background: var(--color-gray-light);
		color: var(--color-gray-dark);
	}
	
	.hours-row.closed:hover {
		background: var(--color-gray-light);
	}
	
	.day {
		font-weight: 600;
		color: var(--color-text);
		font-size: 1.1rem;
	}
	
	.time {
		font-weight: 500;
		color: var(--color-primary);
		font-size: 1.1rem;
	}
	
	.closed .day,
	.closed .time {
		color: var(--color-gray-dark);
	}
	
	.hours-info {
		display: flex;
		flex-direction: column;
		gap: 1.5rem;
	}
	
	.info-card {
		background: var(--color-gray-light);
		padding: 1.5rem;
		border-radius: var(--border-radius-lg);
		text-align: center;
		transition: var(--transition);
	}
	
	.info-card:hover {
		transform: translateY(-3px);
		box-shadow: var(--shadow);
	}
	
	.info-icon {
		font-size: 2rem;
		margin-bottom: 1rem;
	}
	
	.info-card h3 {
		color: var(--color-primary);
		margin-bottom: 0.5rem;
		font-size: 1.2rem;
	}
	
	.info-card p {
		color: var(--color-text-light);
		font-size: 0.9rem;
		line-height: 1.5;
	}
	
	@media (max-width: 768px) {
		.hours-content {
			grid-template-columns: 1fr;
			gap: 2rem;
		}
		
		.hours-row {
			padding: 1rem;
		}
		
		.day,
		.time {
			font-size: 1rem;
		}
		
		.info-card {
			padding: 1rem;
		}
	}
	
	@media (max-width: 480px) {
		.hours-row {
			padding: 0.75rem;
		}
		
		.day,
		.time {
			font-size: 0.9rem;
		}
	}
</style>
